class Regles {
    constructor(title, text, people, awake, thirsty) {
      this.title = title;
      this.text = text;
      this.people = true; //Should i put a boolean in there instead ?
      this.awake = true; //Should i put a boolean in there instead ?
      this.thirsty = true; // Should i put a boolean in there instead ?
    }
  }
  
  export default Regles;